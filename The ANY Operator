SELECT ProductName FROM Products
    -> WHERE ProductID = ANY (
    ->   SELECT ProductID
    ->   FROM OrderDetails
    ->   WHERE Quantity = 10
    -> );

SELECT ProductName FROM Products
    -> WHERE ProductID = ANY (
    ->   SELECT ProductID
    ->   FROM OrderDetails
    ->   WHERE Quantity > 99
    -> );

SELECT ProductName FROM Products
WHERE ProductID = ANY (
  SELECT ProductID
  FROM OrderDetails
  WHERE Quantity > 1000
);
SELECT ProductName FROM Products
WHERE ProductID = ANY (
  SELECT ProductID
  FROM OrderDetails
  WHERE Quantity >= 5
);
